# a-split-run-keeps-its-own-numbers

A foreign marker dividing an ordered run leaves both fragments on the numbers they already carry,
so renumbering stops rewriting markers on lines the operation never touched
