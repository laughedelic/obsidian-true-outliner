# selection-as-subtree-set

Block selection becomes a run of whole subtrees kept as one contiguous range, with ancestors no longer pulled in at a scope crossing; the contiguity rule that pulls ancestors in is dropped
