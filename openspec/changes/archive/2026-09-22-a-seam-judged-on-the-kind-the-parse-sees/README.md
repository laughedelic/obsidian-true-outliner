# a-seam-judged-on-the-kind-the-parse-sees

A boundary normalized before the encode is separated on the kind the re-parse will read, so a
node an operation re-indents past the opening margin stops taking the node below it
