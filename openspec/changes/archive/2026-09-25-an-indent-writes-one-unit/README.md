# an-indent-writes-one-unit

A node an indent or outdent moves is written in its destination's indentation characters on
every line it owns, not only its first, so a tab-indented vault stays tab-indented below a
node's first line
