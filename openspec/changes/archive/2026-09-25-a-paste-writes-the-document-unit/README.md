# a-paste-writes-the-document-unit

A pasted subtree is written in the document's own indent unit at every level, whatever unit the
clipboard used, so a paste from outside the vault no longer leaves the document indented two
ways.
