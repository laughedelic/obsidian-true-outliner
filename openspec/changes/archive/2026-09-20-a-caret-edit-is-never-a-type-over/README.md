# a-caret-edit-is-never-a-type-over

A replacement the editor synthesizes around a caret — Obsidian's own Enter inside a quote — is no
longer read as a structural paste, so the quote keeps its lines instead of collapsing to the
character before the caret
