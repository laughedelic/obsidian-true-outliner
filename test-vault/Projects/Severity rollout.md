# Severity rollout

The severity-first ordering, shipped behind a flag. This note is what the
rollout diary and the long-chain notes point at.

- ordering is stable sort on severity, then recency
- the flag is `--severity-first`
- touch fallback is still open
