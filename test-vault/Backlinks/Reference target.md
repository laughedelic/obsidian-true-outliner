The target for the marker-alignment fixtures.

Deliberately short and referenced only by `Kinds gallery` and `Family tree`, so
its footer is exactly those two notes and fits on one screen — the hub note's
footer is the volume case, not the legibility one.
