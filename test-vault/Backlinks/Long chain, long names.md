A note whose ancestors are deliberately awkward to draw: very long names, a
name that is nothing but a link, and a line carrying several of them.

- [a single link that is the whole of this ancestor's text, long enough that it has to wrap somewhere](https://example.com/a/very/long/path/that/keeps/going/for/a/while)
	- three at once: [first](https://example.com/1), [[Severity rollout|second]] and [third](https://example.com/3) on one line
		- an ancestor whose own text runs long enough to wrap the lineage row on any reasonable pane width, which is the case that only misbehaves at length
			- the leaf finally mentions [[Severity rollout]]

- **a heading-shaped ancestor**

# A real heading with `code`, a [link](https://example.com) and #tag

Body under the heading, so the heading is an ancestor rather than a leaf.

- deeper still
	- mentions [[Severity rollout]] from under the heading
