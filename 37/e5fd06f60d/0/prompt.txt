interesting questions. 

* first, about the mentioned D1. intuitively, I'm leaning towards option A because any subtree should be able to be replanted in any place that is expressible as Markdown. but I understand the conundrum with the losslessness intent. I think it's not a big deal: if a paste happens by mistake, it can be undone. a trickier problem is the paste the other way: trying to paste a list subtree into a heading prefix: should it become a heading or replace the heading prefix as a normal list? maybe we need two types of paste operation? I don't know yet. I think we are in an uncharted territory here, since other outliners don't deal with non-list types of trees.
* for the variable depth selection indent: my intuition would be to just indent each of the subtrees independently, some might reject, some will change relative indentation. seems like a predictable UX. but I tried it in Logseq and it just rejects  it if one of the selected nodes can't be indented. at the same time, it can outdent such selection, which also breaks relative indentation (so then what's the point of rejecting "partial" indentation?)
* I think paste issue is less urgent than the structural ops. but I don't mind any order of implementation. whatever makes most sense scoping-wise.