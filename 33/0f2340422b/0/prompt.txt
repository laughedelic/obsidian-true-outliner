All decoration-experiment PRs (#8, #10, #11) are squash-merged into `main`; the chosen implementation (Experiment 1 additive indentation + Experiment 2b CSS-gradient guides + Experiment 5a SVG icon block markers) is live on `main`. Your job is the SDD backfill: complete **task 0.6** of the paused openspec change `outline-decorations` — reconciling its planning artifacts with what was actually built — using `/openspec-update-change`. This is a docs/spec-only PR: create a branch off `main` (suggest `docs/openspec-backfill-decorations`), no code changes.

Read first, in this order: `openspec/changes/outline-decorations/` (proposal.md, design.md, tasks.md, specs/outline-decorations/spec.md — note the "Paused for experiments" annotations and task 0.6's own instruction), then `docs/research/07-decoration-experiments-plan.md` (series hub + results table), `10-experiment-5-block-markers.md` (the 5a-vs-5b verdict and the "Next steps: hardening 5a" checklist), and `11-decoration-lessons.md`. Docs 08/09 as reference for Experiments 1/2b details. Write everything in neutral team voice (we/our, "manual review"/"a tester", never you/your or session-log phrasing — see the repo's established convention in those docs).

Produce:

1. **design.md**: rewrite to record the *chosen* architecture as built — the three-layer decoration system (additive-only indentation; stacked-gradient guides on `::after`; SVG marker widgets + widget-atom DOM injection + `MarginCompensation` ViewPlugin; DOM-ancestry `isNestedEditor` gating). Link docs 07–11 for rationale and history; do not duplicate them.

2. **specs/outline-decorations/spec.md**: rewrite as behavioral requirements derived from the validated invariants, not implementation history. The invariants are enumerated across docs 08–11 and the e2e suites; the core set: a pure list (no non-list ancestors) renders byte-identical to outline-mode-off; native list `text-indent`/`padding-left` are never touched (additive-only); list items get no guide and no marker; guides render only where native indent guides have no representation, and run through the marker's center; a marker appears on a node's first line only, never continuation or gap lines; widget-replaced atoms (table/callout/hr/html) are indented/marked via direct DOM patching with native padding compensated; Obsidian's readable-line-width base margin is added to, never replaced; nested per-cell editors receive no decorations; the `markerVisibility` setting semantics (all / with-children / headings-and-paragraphs); table horizontal scroll keeps working with guides active.

3. **tasks.md**: check off section 0 (experiments — all concluded, link the results table). Rewrite sections 1–3 as (a) done-and-verified items mapping to what landed, with pointers to the merged PRs and covering tests (`tests/decorate.test.ts`, `e2e/specs/50/51/52*.e2e.ts`), and (b) remaining work = the hardening checklist from doc 10: fold-chevron live measurement (port 5b's approach), evaluate `app.workspace.updateOptions()` to replace the forceRedraw off/on hack, consolidate to one shared `parse()`/`decorate()` pass per transaction, `aria-hidden` on marker SVGs + RTL/IME/community-theme passes, adopt the declarative settings API (`getSettingDefinitions` — the one remaining lint warning), protect the two documented invariants (no children appended into `.cm-line`; widget-subtree injection). Add a placeholder task for a mobile-emulation verification pass (blocked on the mobile e2e PR #7 landing). Fold in the open question from doc 10 (shrinking only our own added list margin) as an explicitly undecided item.

4. **Traceability pass** (the actual validation): walk requirement ↔ covering test ↔ code site in both directions. A code behavior with no requirement is an undocumented decision — spec it or flag it; a requirement with no covering test is a coverage gap — record it as a task. Report the orphans found in the PR description.

5. One-line status update in `docs/research/10-experiment-5-block-markers.md`'s hardening checklist: item 5 (eslint-plugin-obsidianmd violations) was resolved on `main` (commit "fix: resolve eslint-plugin-obsidianmd violations in Experiment 5a marker code"); only the `getSettingDefinitions` warning remains, now tracked as its own task.

Run `openspec validate` on the change; verify all relative links in touched files resolve. Do NOT run `openspec sync-specs` or archive the change — the change stays open until the hardening tasks land; syncing/archiving is a later, deliberate step. Open a draft PR → `main`. Do not commit anything beyond the openspec change dir, the one doc-10 status line, and (if needed) `docs/research/README.md`.