legacy-peer-deps=true

# `npm version` bumps manifest.json/versions.json; the release is cut by CI from
# the merge commit that lands them, so a local tag would name a commit main never sees.
git-tag-version=false
