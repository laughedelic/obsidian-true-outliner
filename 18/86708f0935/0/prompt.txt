This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:

This is an ongoing `/openspec-apply-change outline-edit-enforcement` implementation session (OpenSpec spec-driven workflow) for an Obsidian plugin ("True Outliner") that maps markdown documents to an outline tree and enforces structural editing semantics. The session has proceeded through the full 21-task implementation plus FIVE rounds of real-vault manual-pass feedback from the user, each following a strict "docs first, then implementation, then status report" workflow the user explicitly established:

- Round 1 (initial): Implement all 21 tasks from `tasks.md` (pure ops, verdict layer, funnel wiring, e2e suite, verification) — completed, but real-vault manual pass (5.2) explicitly requires the user's own hands/judgment (subjective feel), which I flagged as blocking.
- Round 2 (first manual pass): User reported bugs from actually using the plugin — spawned the "chrome-transparency" principle (gap lines/markers are storage chrome; edit semantics must not depend on them) and required implementing D9-D11 amendments (merge table rewrite, split-with-children fix, cursor-derived merge recognition).
- Round 3 (second manual pass): User reported a merge cursor bug (D12) and asked for my "honest take" on whether cursor NAVIGATION (not just edits) should also respect chrome-transparency. User explicitly said: "before you jump into the implementation, make sure that the SDD documentation is updated and record the ideas for the follow up work somewhere as well... docs first, then implementation of what we agreed on here, then the status on the completion of the change." User agreed to land marker-transparency now (D13) and defer gap-line cursor navigation to a future change combined with selection-UX work.
- Round 4 (third manual pass): User reported paste bugs (single-node-with-children never re-indexed — D15) and a redo-cursor bug and a delete→undo→redo-cursor-on-chrome bug. I fixed D15, could not reproduce the redo bugs despite 5+ e2e attempts, and asked for more specific repro info, explicitly documenting: "Recorded rather than silently dropped... more specific repro steps requested from the user."
- Round 5 (fourth/fifth manual pass, MOST RECENT): User reported the D15 fix was incomplete ("the re-indentation math must be still partially broken... at the +2 depth or more, it resets back to the original") and gave an EXACT, precise redo-cursor repro:
  ```
  paragraph A
  
  paragraph B
  ```
  "1. cursor at the start of paragraph B. 2. press backspace. paragraphs merged, cursor between the merged paragraphs (after `A`). 3. undo (cmd-Z), initial state. cursor at the start of paragraph B, same as at the start. 4. redo (cmd-shift-Z), same merge. but the cursor is on the next (gap) line. this is the problem! nothing else happening between the steps. macOS, desktop Obsidian, using keyboard."

  The user's explicit instructions for this round were: "try to figure out why the testing harness is not detecting this behavior. and try to figure the reason for this behavior from the current implementation, even if you can't reproduce it. if you need any more debug information I can provide through manual testing, let's do it." — and separately: "for the paste bug, see `Paste bug repro` in the test-vault. let me know if that is enough to understand the problematic behavior."

The overarching intent throughout: implement `outline-edit-enforcement` (Phase C of a multi-phase outline-mode enforcement system) correctly, validate rigorously via automated tests AND real-vault manual passes, treat the user's manual-pass findings as authoritative bug reports to root-cause (not just patch symptomatically), keep OpenSpec SDD documentation (design.md, specs, tasks.md) and `docs/research/` findings synchronized with every code change, and be transparent when something cannot be reproduced rather than guessing/pretending to fix it.

2. Key Technical Concepts:
   - OpenSpec spec-driven development workflow (proposal/design/specs/tasks artifacts, delta specs with ADDED/MODIFIED/REMOVED requirements, `openspec validate --strict`, `openspec status`/`openspec instructions apply`)
   - CodeMirror 6 (CM6) `EditorState.transactionFilter` as the enforcement choke point; `Transaction.userEvent`, `Transaction.addToHistory`, `StateEffect`, `ChangeSet`/`ChangeSpec`, ChangeSet position mapping
   - CM6's `@codemirror/commands` `history.ts` internals: `HistEvent` with `startSelection`/`selectionsAfter` fields; undo restores `event.startSelection` (pre-edit selection) and pushes a new "undone" event whose `startSelection` is the selection active "at the moment undo runs"; redo restores THAT captured selection directly (no remapping)
   - The outline block-tree model (`OutlineDoc`/`OutlineNode`, `src/model.ts`): nodes own their lines verbatim; blank-line "gaps" are owned by the preceding node (`trailingGap`); `preamble` for frontmatter
   - Pure/impure layering discipline: pure core modules (`ops.ts`, `enforce.ts`, `classify.ts`, `escalate.ts`, `parse.ts`, `encode.ts`, `reencode.ts`, `rules.ts`, `result.ts`) have NO CodeMirror/Obsidian imports; only `src/plugin/*` adapts to CM6/Obsidian
   - Transaction classification taxonomy (6 classes: programmatic, composition, plugin-own, selection-only, within-node-edit, boundary-crossing-edit) — default-permit at the taxonomy level, narrow default-deny only inside boundary-crossing-edit
   - Verdict layer: `pass | rewrite | veto` computed purely from `(class, doc, EditFact)`
   - "Chrome transparency" principle (D9): gap lines and list markers are markdown-encoding-layer chrome; editing semantics must never depend on gap width/ownership or marker internals; recognition of edit INTENT must come from the pre-edit cursor position (`cursorBefore`), not just the raw edit shape, since chrome-crossing edits can be byte-identical to deliberate-chrome edits
   - Content-adjacent merge (D10) and split (D11): merges/splits computed relative to CONTENT boundaries, not raw character positions
   - Marker-transparent cursor placement (D13): `clampCursorToContent` redirects any cursor-only selection landing inside a list item's marker prefix to content-start, scoped ONLY to list markers (not gap lines, not headings)
   - `reindentSubtreeVerbatim` / `reencodeBlocksForDestination` (D15/D16): re-indenting a copied/pasted subtree by swapping the top node's OWN leading-whitespace PREFIX for the destination's indent text (string-prefix swap), preserving descendants' relative indent strings verbatim — avoids `shiftSubtree`'s flat-numeric-column-delta approach which mismatches indent units (tabs vs. spaces) at depth
   - `isStructuralBlockSequence`: shared predicate (more than one top-level block, OR one block with children) used by BOTH `classify.ts` (to decide if a transaction should even reach the verdict layer) and `enforce.ts` (to decide if a paste needs structural splicing) — a recurring bug pattern in this session was "two call sites for one rule, only one kept current"
   - `deleteSubtrees`/`mergeNodes`/`insertSubtrees` are OPS whose output (`OpOutput.doc`) always comes from a FRESH `parse()` reparse inside `finalize()` — meaning node IDs from the pre-reparse "surgery" tree never match IDs in the returned `.doc`. This caused multiple real bugs when code tried to look up nodes by their OLD id in the NEW doc; the fix pattern established is to look up by LINE POSITION (via `nodeAtLine`) instead, or to only ever reference `finalize`'s own pre-computed cursor.
   - Vitest unit/property testing (fast-check `fc.assert`/`fc.property`), `tests/generators.ts`'s `arbTree()` generator
   - WebdriverIO/`wdio-obsidian-service` e2e testing against a REAL (sandboxed) Obsidian instance; `browser.keys()`, `mouseDragSelect`, `dispatchSelectOnlyRanges`, `pasteText`, `h.getStats()`/`h.getCursor()`/`h.getSelection()`/`h.getBuffer()` helpers
   - Known e2e flakiness categories already documented in the codebase: real-mouse-drag timing (`mouseDragSelect`), and (newly observed) nested-table-cell click interactability — both environmental, not code regressions
   - Mobile emulation testing (`OBSIDIAN_E2E_MOBILE=1`, `wdio.mobile-emulation.conf.mts`) — `IS_MOBILE_RUN` guards skip real-pointer-based tests
   - Memory system: persistent memory files at `/Users/laughedelic/.claude/projects/-Users-laughedelic-Code-obsidian-true-outliner/memory/`, indexed via `MEMORY.md`; created `chrome-transparency-editing.md` as a feedback-type memory this session

3. Files and Code Sections:

   - **`src/ops.ts`** (core pure structural operations)
     - Added `deleteSubtrees(doc, nodeIds)`, `mergeNodes(doc, firstId)`, `insertSubtrees(doc, anchorId, parsedBlocks, position)` as the three new Phase C ops.
     - `finalize()` made to accept `subjectId: number | undefined` and exported.
     - `destinationIndent` and `contentColumnCh` exported (were private).
     - `mergeNodes` was REWRITTEN multiple times: first version (single-separator, simple table), then D10 rewrite (cross-kind joins, children re-parenting via `childBaseCol`/`shiftSubtree`, single-line heading absorption), then D12 fix (cursor at join point):
       ```ts
       const result = finalize(doc, surgery, merged.id);
       if (!result.ok) return result;
       const joinLineOffset = first.kind === 'heading' && first.setext ? 0 : first.lines.length - 1;
       const joinLine = result.value.cursor.line + joinLineOffset;
       const joinCh = (first.lines[joinLineOffset] ?? '').length;
       return accept({ ...result.value, cursor: { line: joinLine, ch: joinCh } });
       ```
     - `splitNode` rewritten (D11) so a node WITH children places the split remainder as the new FIRST CHILD.
     - Most recent addition — `reindentSubtreeVerbatim` (exported) and `reencodeBlocksForDestination` (exported, new shared function extracted from `insertSubtrees`):
       ```ts
       export function reindentSubtreeVerbatim(node: OutlineNode, indentText: string): OutlineNode {
         const topWs = leadingWhitespace(node.lines[0] ?? '');
         const swapLine = (line: string): string => {
           if (line.trim() === '') return line;
           const ws = leadingWhitespace(line);
           return ws.startsWith(topWs) ? indentText + line.slice(topWs.length) : line;
         };
         const recur = (n: OutlineNode): OutlineNode => ({
           ...n,
           lines: n.lines.map(swapLine),
           children: n.children.map(recur),
         });
         return recur(node);
       }

       export function reencodeBlocksForDestination(
         doc: OutlineDoc,
         parent: OutlineNode | 'root',
         precedingSiblings: readonly OutlineNode[],
         followingSiblings: readonly OutlineNode[],
         parsedBlocks: readonly OutlineNode[],
       ): readonly OutlineNode[] {
         const indentText = destinationIndent(doc, parent, precedingSiblings);
         const newContentKind = encodingKindAtDestination({
           parentKind: parent === 'root' ? 'root' : parent.kind,
           precedingSiblings,
           followingSiblings,
         });
         return parsedBlocks.map((block) => {
           const isContentBlock = block.kind === 'paragraph' || block.kind === 'list-item';
           if (!isContentBlock || newContentKind === block.kind) {
             return reindentSubtreeVerbatim(block, indentText);
           }
           return reencodeForDestination(block, newContentKind, indentText);
         });
       }
       ```
       `insertSubtrees` now calls `reencodeBlocksForDestination(doc, parent, precedingSiblings, followingSiblings, parsedBlocks)` instead of inline mapping logic.

   - **`src/enforce.ts`** (pure verdict layer)
     - Contains `EditFact`, `Verdict`, `computeVerdict`, `recognizeMergeIntent` (D10 rewrite, cursor-derived), `computeMergeVerdict`, `computeDeletionVerdict`, `computePasteVerdict`, `composeTypeOver`, `deleteAndSplice` (shared helper extracted from composeTypeOver), `isEmptyAnchor` (D14), `endOfInsertedRun`, `siblingCoverIds`, `survivorsOf`, `coverIdsOf`.
     - `computePasteVerdict` uses `isStructuralBlockSequence` (imported from classify.ts) instead of `parsedBlocks.length <= 1`.
     - MOST RECENT FIX — `insertAsOnlyChildren` (D16), previously never re-indented at all, now:
       ```ts
       function insertAsOnlyChildren(
         doc: OutlineDoc,
         parentPath: readonly number[],
         parsedBlocks: readonly OutlineNode[],
       ): OpResult<OpOutput> {
         const parent = parentPath.length === 0 ? 'root' : nodeAt(doc, parentPath)!;
         const reencoded = reencodeBlocksForDestination(doc, parent, [], [], parsedBlocks);
         const rebuild = (nodes: readonly OutlineNode[], depth: number): readonly OutlineNode[] => {
           if (depth === parentPath.length) return reencoded;
           const index = parentPath[depth]!;
           return nodes.map((node, i) =>
             i === index ? { ...node, children: rebuild(node.children, depth + 1) } : node,
           );
         };
         const surgery: OutlineDoc = { ...doc, children: rebuild(doc.children, 0) };
         return finalize(doc, surgery, reencoded[0]?.id);
       }
       ```
     - Imports updated: `import { findPath, nodeAt } from './model';` and `reencodeBlocksForDestination` added to the ops.ts import list.

   - **`src/classify.ts`**
     - `ChangedLineSpan` extended with optional `insertedText`, `deletesLineBoundary`, `fromCh`, `toCh`.
     - `TransactionFacts` extended with optional `cursorBefore`.
     - New exported `isStructuralBlockSequence(parsedBlocks)` predicate: `parsedBlocks.length > 1 || (parsedBlocks.length === 1 && parsedBlocks[0]!.children.length > 0)`.
     - `isMultiBlockInsertion` uses `isStructuralBlockSequence(parse(span.insertedText).children)`.
     - `crossesViaBoundaryDeletion` added for the single-char-deletion-degenerate-span case.
     - `PLUGIN_OWN_USER_EVENTS` extended with `'delete.structural'`, `'input.paste.structural'` (D7a short-circuit).

   - **`src/escalate.ts`**
     - New exported `clampCursorToContent(doc, pos)` (D13) — imports `contentColumnCh` from `./ops`.
     - Module doc comment updated to describe D13's narrower cursor-touching exception.

   - **`src/plugin/transaction-filter.ts`**
     - `collectChangedLineSpans` extended to compute `insertedText`, `deletesLineBoundary`, `fromCh`, `toCh`.
     - `collectEditFact` added, includes `cursorBefore: offsetToLinePos(tr.startState.doc, tr.startState.selection.main.head)`.
     - `buildRewriteSpec` constructs the replacement TransactionSpec (changes/selection/userEvent/`Transaction.addToHistory.of(true)` annotation).
     - `vetoEffect` StateEffect + `EditorView.updateListener` for the rejection-cue side effect (kept outside the filter itself).
     - `escalateSelection` updated to ALSO run `clampCursorToContent` on empty (cursor) ranges after `escalateRanges`.

   - **`src/plugin/stats.ts`** — `VerdictKind` type, `recordVerdict()`, `verdictCounts`/`verdictTiming` in `StatsSnapshot`.
   - **`src/plugin/messages.ts`** — new `REJECTION_MESSAGES` entries for new `RejectionReason` values.
   - **`src/result.ts`** — new `RejectionReason` union members (`empty-selection`, `non-contiguous-subtrees`, `no-following-neighbor`, `would-orphan-children`, `merge-not-expressible`, `insertion-not-expressible`).

   - **Tests** (all extended/added): `tests/edit-ops.test.ts`, `tests/enforce.test.ts`, `tests/escalate.test.ts`, `tests/classify.test.ts`, `tests/grammar.test.ts`, `tests/stats.test.ts`.
     - Most recent addition — `tests/enforce.test.ts`, exact byte-for-byte regression for the vault repro:
       ```ts
       it('pasting into an empty item that is the ONLY (sole) child at a deep level re-indents to that depth, not top level (D16 regression)', () => {
         const md = '- parent1\n\t- child1\n\t- child2\n- parent2\n\t- plus one level\n\t\t- plus two levels\n\t\t\t- \n';
         const doc = parse(md);
         const emptyLine = md.split('\n').findIndex((l) => l === '\t\t\t- ');
         const edit: EditFact = {
           from: pos(emptyLine, '\t\t\t- '.length),
           to: pos(emptyLine, '\t\t\t- '.length),
           insert: '- parent1\n\t- child1\n\t- child2\n',
         };
         const verdict = computeVerdict('boundary-crossing-edit', doc, edit);
         expect(verdict.kind).toBe('rewrite');
         const text = applyVerdict(md, verdict);
         expect(text).toBe(
           '- parent1\n\t- child1\n\t- child2\n- parent2\n\t- plus one level\n\t\t- plus two levels\n\t\t\t- parent1\n\t\t\t\t- child1\n\t\t\t\t- child2\n',
         );
       });
       ```

   - **`e2e/specs/62-outline-edit-enforcement.e2e.ts`** — the main growing evidence suite (33+ scenarios by the latest count). Most recent addition:
       ```ts
       it('pasting into an empty item that is the SOLE child at a deep level re-indents there, not top level (D16, real-vault repro)', async function () {
         await outlineNote(
           '- parent1\n\t- child1\n\t- child2\n- parent2\n\t- plus one level\n\t\t- plus two levels\n\t\t\t- \n',
         );
         await h.setCursor(6, '\t\t\t- '.length);
         await h.pasteText('- parent1\n\t- child1\n\t- child2\n');
         expect(await h.getBuffer()).toBe(
           '- parent1\n\t- child1\n\t- child2\n- parent2\n\t- plus one level\n\t\t- plus two levels\n\t\t\t- parent1\n\t\t\t\t- child1\n\t\t\t\t- child2\n',
         );
       });
       ```

   - **`test-vault/Paste bug repro.md`** — a user-authored repro note (read-only, not modified by me) containing Input/Action/Current-result/Expected-outcome sections that gave the EXACT repro for the D16 bug. Content (verbatim, tabs shown as literal tab characters):
     ```
     ## Input
     
     - parent1
     	- child1
     	- child2
     - parent2
     	- plus one level
     		- plus two levels
     			- 
     
     ---
     
     ### Action
     
     In the outline mode
     1. copy parent1 subtree
     2. paste at the _empty node_ under "plus two levels"
     
     ## Current result
     
     - parent1
     	- child1
     	- child2
     - parent2
     	- plus one level
     		- plus two levels
     - parent1
     	- child1
     	- child2
     
     ---
     
     ## Expected outcome
     
     
     - parent1
     	- child1
     	- child2
     - parent2
     	- plus one level
     		- plus two levels
     			- parent1
     	            - child1
     	            - child2
     ```

   - **OpenSpec docs** — `openspec/changes/outline-edit-enforcement/design.md` now has D1 through D16 (most recent: D15 "structural-paste detection and re-indentation both corrected", D16 "the empty-anchor paste fallback never re-indented at all"); `proposal.md`; `tasks.md` now has sections 1-9 (most recent: section 9 "Fifth manual-pass findings" with 9.1 = D16 fix done, 9.2 = redo investigation done/documented); `specs/node-edit-enforcement/spec.md` (added scenario "Replacing the SOLE empty child at a deep level re-indents to that depth"); `specs/structural-operations/spec.md`; `specs/node-selection-enforcement/spec.md` (new delta, D13); `specs/outline-keyboard-grammar/spec.md` (new delta, D11); `specs/e2e-verification/spec.md` (new delta).

   - **`docs/research/04-open-questions.md`** — appended Q15 (chrome-transparency findings), Q16 (round 2 pass), Q17 (outdent-in-place / heading-Enter-split pending decisions), Q18 (D15 paste fix + first redo investigation, not reproduced), and MOST RECENTLY **Q19** ("Fifth manual pass — paste-depth root cause found and fixed; redo-cursor mechanism understood but still unreproduced") documenting: the D16 root cause and fix in detail, and the full CM6 history.ts mechanism (undo restores `event.startSelection`; the "undone" stack's new event's `startSelection` is captured at the moment undo fires; redo restores that directly with no remapping), PLUS the empirical confirmation that `stats.snapshot().counts.programmatic` never increments for undo OR redo (proving our code cannot be the direct cause), plus a working hypothesis and specific next diagnostic questions for the user (minimal-vault test, Obsidian version, non-default settings, mouse-click-vs-keyboard cursor placement).

   - **`docs/research/12-decoration-follow-ups.md`** and **`docs/research/13-selection-follow-ups.md`** — updated in round 3 with the deferred gap-line-cursor-transparency/gap-hiding scope notes.

   - **Memory**: `/Users/laughedelic/.claude/projects/-Users-laughedelic-Code-obsidian-true-outliner/memory/chrome-transparency-editing.md` created (feedback-type memory) plus `MEMORY.md` index updated.

4. Errors and fixes:
   - **Stale pre-reparse node-id bug** (multiple instances): `finalize()` always does a FRESH `parse()`, so `OpOutput.doc`'s ids never match the surgery tree's ids. Caused (a) type-over cursor placement bug (fixed via `endOfInsertedRun` computing position by LINE + sibling offset, not id) and (b) `composeTypeOver`'s survivor lookup bug (fixed via `nodeAtLine(doc2, deletion.value.cursor.line)` instead of stale id). User feedback: none directly on this (self-caught via testing), but it established the durable pattern "look up by line position, never by id, across a `finalize()` boundary."
   - **Ancestor-descendant selection cover bug**: selecting a single node together with its own subtree hit "Nothing to act on" veto because `siblingCoverIds`/cover math didn't mirror `escalate.ts`'s own ancestor-descendant scope-resolution fallback. Fixed by mirroring that logic. Found during round 1 manual pass, confirmed via e2e regression test.
   - **Backspace/Delete degenerate span bug**: a literal single-character deletion always has `fromLine === toLine` under the existing `Math.max(fromA, toA-1)` line-span convention, meaning it could never classify as `boundary-crossing-edit` without a NEW fact. Fixed by adding `deletesLineBoundary` to `ChangedLineSpan` and `crossesViaBoundaryDeletion` to classify.ts.
   - **Chrome-blocked merges** (round 1 manual pass, user-reported): Backspace across a gap needed one keystroke per blank line; marker-space Backspace corrupted the marker instead of merging; Enter-with-children jumped past the whole subtree. Root cause: verdict layer recognized intent in "markdown character space" not "outline content space." Fixed via D9-D11 (cursor-derived `cursorBefore` fact, rewritten merge table, content-adjacent split).
   - **Mobile-emulation-only e2e flake**: `dispatchSelectOnlyRanges` immediately followed by a keypress didn't survive to the keypress under mobile Chrome emulation (real dispatch quirk, not a code bug) — fixed by switching that specific test to use `setSelection` (also more representative of the D3 stale-selection path).
   - **Merge cursor bug** (round 2, user-reported): `mergeNodes` reused `finalize`'s generic "node start" cursor convention instead of computing the actual join point. Fixed (D12) as shown above, with a unit test verifying a multi-line `first` case specifically (so it's not a single-line coincidence).
   - **Paste detection gate bug** (round 3, user-reported "literal `\t\t-` prefix... single top-level node"): both `isMultiBlockInsertion` and `computePasteVerdict` required `parsedBlocks.length > 1`, so a single node WITH children never reached the rewrite path. Fixed via shared `isStructuralBlockSequence`.
   - **Re-indentation unit-mismatch bug** (round 3, discovered while investigating the above): `shiftSubtree`'s numeric-delta approach added SPACES for any depth shift, mismatching a tab-indented subtree's descendants. Fixed via `reindentSubtreeVerbatim` (string-prefix swap).
   - **`insertAsOnlyChildren` never-reindented bug** (round 5/D16, user-provided exact repro note): the "no surviving sibling at all" fallback path for paste-into-empty-anchor spliced parsed blocks in completely VERBATIM with no re-indentation — a case NONE of the D15 regression tests exercised (they all used anchors with at least one real sibling). Root-caused by reading my own old code comment ("Blocks land un-reindented (an empty scope has no destination context to adopt)") and recognizing the reasoning was WRONG (the parent node itself has a well-defined depth). Fixed by extracting `reencodeBlocksForDestination` and using it in `insertAsOnlyChildren` too. Confirmed byte-for-byte against the user's own "Expected outcome" in the repro note.
   - **Redo-cursor bug — NOT reproduced, but mechanism understood**: Despite 7+ varied automated e2e repro attempts (including the user's EXACT literal content/steps, with and without pauses), every attempt showed CORRECT cursor restoration on redo. I researched the actual CM6 `history.ts` source via WebFetch (working around a copyright/quote-length guardrail by asking narrow, targeted questions rather than requesting full file reproduction) and empirically confirmed via a temporary e2e test (`debug7.e2e.ts`, later deleted) that `stats.snapshot().counts.programmatic` never increments for EITHER undo or redo — proving these transactions never reach `transactionFilter` at all, so our own code cannot be the direct cause of a wrong cursor. This is UNRESOLVED — documented as Q19/task 9.2, with specific follow-up questions posed to the user (minimal-vault test with only true-outliner enabled; Obsidian version; non-default editor settings; whether initial cursor placement was via mouse click vs. keyboard) rather than continuing to guess.
   - **My own test-writing mistakes** (self-caught, not user-reported): several unit tests I wrote initially had wrong expected values due to my own arithmetic errors (e.g., the first D16 unit test's `not.toMatch(/^- parent1$/m)` assertion false-matched the ORIGINAL source content's "- parent1" line rather than the newly-pasted one) — caught immediately via test failure output and corrected to an exact full-string `toBe()` assertion instead.
   - **Flaky e2e tests** (environmental, not code bugs): (a) nested-table-cell-click tests (`53-decoration-contracts`, `60-transaction-classification`) failed consistently for one stretch, traced to likely interference from the user's own long-running real Obsidian.app on the same test-vault (untouched code path, confirmed via `git diff`); (b) `61-selection-enforcement.e2e.ts`'s "forward mouse drag" test failed once in the most recent full run, confirmed flaky (real-mouse-drag timing) via 3x isolated reruns all passing. Both explicitly NOT treated as code regressions, documented as such in `tasks.md`.

5. Problem Solving:
   - Established and repeatedly applied the debugging pattern: when a manual-pass bug can't be immediately explained, WRITE A DIRECT UNIT TEST reproducing the exact scenario (via `computeVerdict`/`insertSubtrees` calls with hand-built `EditFact`/tree inputs) BEFORE touching implementation code, to confirm the failure mode precisely, then fix, then re-verify the same test passes.
   - For the redo-cursor investigation specifically, pivoted from "keep trying more e2e repro variations" (which had been unproductive after 7+ attempts) to "read the actual upstream CM6 source to understand the mechanism precisely" — a more principled research-driven approach that yielded concrete understanding (CM6's `history.ts` selection-restoration semantics) even without achieving reproduction, and separately used a targeted e2e test checking `stats` counters (not just buffer/cursor) to empirically PROVE our code path is not involved.
   - Consistently followed the "docs first, implementation second, status report third" workflow the user established, updating design.md/specs/tasks.md/docs-research BEFORE or alongside each code fix, and validating with `openspec validate outline-edit-enforcement --strict` after each doc change.
   - Consistently ran full verification after each fix: `npx tsc --noEmit` (both root and `-p e2e`), `npx eslint src tests`, `npx vitest run` (full suite), then the FULL e2e suite via `npx wdio run e2e/wdio.conf.mts` run TWICE consecutively in the background (using `run_in_background: true` + polling via scheduled wakeups, per the harness's guidance to avoid blocking on long-running commands), then `npm run vault:install` to rebuild and redeploy to the test vault.
   - Currently mid-verification for the D16 fix: first full e2e run showed one flaky (unrelated) failure, confirmed flaky via isolated reruns, and a SECOND full consecutive run was just launched (background task `b3iddzhii`, output to `/tmp/e2e-d16-run2.log`) whose result has not yet been checked.

6. All user messages (verbatim, non-tool-result, user-role turns only):
   - "ok. this is a big change. I need to verify it carefully. * first, I noticed a bug (might be pre-existing): a paragraph with 1-3 preceeding spaces gets very misaligned... [detailed bug report about decoration misalignment, heading+subtree backspace veto, gap-line interference discussion proposing chrome-transparency, list marker corruption on backspace, Enter-with-children creating wrong sibling structure] ... I want you to think about these things as a whole, not just a list of cases to test and fix, but as indicators of the current system not behaving well. consider whether the spec itself needs an adjustment, whether the underlying system needs to change, or whether there is still a gap in our fundamental functionality that prevents us from getting the natural feel of working with an outline. it's probably a combination of all the factors."
   - "go on" / "go on" (brief continuations)
   - "okay. great. here are some more observations: * one thing that is the consequence of the adjustment I requested before is that splitting or adding a new node after one with children introduces a new child. in some cases this introduces a 'soft-lock'... [detailed description of the merge→split→outdent soft-lock, proposing 'outdent in place' matching Logseq semantics] ... * pressing enter anywhere in a heading jumps out of it and adds a gap line. is this intended behavior?... * adjustment to the tree pasting: if I copy/cut a list subtree, then create a new node in the list and paste, that new empty node should be treated as the pasting anchor... * something to think about, not necessarily act on immediately: what if we treat headings `#` chrome the same way as the lists bullets?... * I'm not sure what else to try and verify. do you have any candidates for questionable feel that I need to validate with manual vibe-testing?"
   - "ok. good arguments. let's go ahead with the recommendation. the work on hiding/_visually_-normalizing gaps and the related vertical cursor navigation could go in a separate change and potentially combine with the selection UX work that's been already set for later. but before you jump into the implementation, make sure that the SDD documentation is updated and record the ideas for the follow up work somewhere as well (potentially make the @docs/research/12-decoration-follow-ups.md, or broaden @docs/research/13-selection-follow-ups.md, or a new doc, your call). docs first, then implementation of what we agreed on here, then the status on the completion of the change. I will give it another pass and we'll continue from there."
   - "okay. great. here are some more observations: * one thing that is the consequence of the adjustment I requested before is that splitting or adding a new node after one with children introduces a new child..." [NOTE: this appears to be a duplicate/re-send in the transcript of the prior message — treated as such]
   - "ok. great. here are some more observations..." (the message containing the soft-lock/outdent/heading-Enter/paste-anchor/heading-chrome/vibe-testing-candidates request — already summarized above)
   - "okay. good arguments..." (the go-ahead message — already summarized above)
   - "there is an improvement. but the re-indentation math must be still partially broken. pasting a subtree at lower depth, same as original, or +1 works. but at the +2 depth or more, it resets back to the original. check that please, --- redo-cursor: still broken and reliably reproducible in real UI. example: ``` paragraph A / paragraph B ``` 1. cursor at the start of paragraph B. 2. press backspace. paragraphs merged, cursor between the merged paragraphs (atfer `A`) 3. undo (cmd-Z), initial state. cursor at the start of paragraph B, same as at the start. 4. redo (cmd-shift-Z), same merge. but the cursor is on the next (gap) line. this is the problem! nothing else happening between the steps. macOS, desktop Obsidian, using keyboard."
   - "Check /tmp/e2e-d15-run2.log (background task bb83x6lkk). If clean, run npm run vault:install, mark tasks.md 8.4 done, and deliver the prepared honest report to the user: D15 (paste detection + re-indentation, two compounding bugs both fixed) verified through unit/property/e2e tests and the real dispatch path, full suite green twice; and the redo-cursor / delete-undo-redo-chrome reports investigated with 5+ varied e2e scenarios each but NOT reproduced — ask for more specific repro steps (exact starting content, exact key sequence, platform, anything else done between steps) rather than guessing further." [Note: this message's content matches a task I had already completed and reported in the immediately preceding turn — I responded that I had already done this.]
   - "I tried restarting the app, reloading the plugin, etc. same result, the cursor placement bug is reproducible every time. try to figure out why the testing harness is not detecting this behavior. and try to figure the reason for this behavior from the current implementation, even if you can't reproduce it. if you need any more debug information I can provide through manual testing, let's do it. ---- for the paste bug, see `Paste bug repro` in the test-vault. let me know if that is enough to understand the problematic behavior"
   - [Most recent, this turn] A strict system-level instruction (not a substantive user request) demanding a TEXT-ONLY summary with no tool calls, which is the prompt currently being responded to.

   No security-relevant instructions or constraints beyond the standard repo-wide git safety rules already in effect (never force-push, never amend without asking, no destructive git ops without explicit confirmation, etc.) — none of these were specifically invoked or overridden in this conversation. The user's global CLAUDE.md preferences (no Co-Authored-By: Claude trailers in commits; 2026 temporal awareness) remain in effect but no commits have been made in this session.

7. Pending Tasks:
   - **Immediate**: Check the result of the second consecutive full e2e verification run (background task `b3iddzhii`, output `/tmp/e2e-d16-run2.log`) for the D16 fix.
   - If clean (or only known-flaky tests fail): run `npm run vault:install`, confirm `tasks.md` sections 9.1/9.2 are accurately marked, and deliver the prepared report to the user covering:
     (a) D16 paste bug — found via their exact repro note, root-caused (empty-anchor-with-no-siblings falls to `insertAsOnlyChildren`, which never re-indented), fixed via `reencodeBlocksForDestination`, confirmed byte-for-byte against their stated "Expected outcome", verified via unit + e2e tests and full suite;
     (b) redo-cursor bug — explain the CM6 `history.ts` mechanism precisely (undo/redo selection restoration semantics), and the empirical proof that undo/redo transactions never reach our `transactionFilter` at all (so our code cannot be the direct cause), and ask the user for specific additional diagnostic info: reproduce in a vault with ONLY true-outliner enabled (isolate other plugins), report exact Obsidian version and any non-default editor settings, and clarify whether the initial cursor placement in their repro was via mouse click or keyboard (the one variable not fully matched in automated attempts).
   - Two OLDER pending decisions remain unresolved and explicitly deferred pending explicit user go-ahead (documented in `tasks.md` 8.2/8.3 and `docs/research/04` Q17), NOT yet acted on:
     - 8.2: `outdent`'s handling of a node with FOLLOWING SIBLINGS under the same parent (confirmed via direct testing to be a pre-existing bug in the CORE `outdent` operation, predating this entire change) — proposed fix is Logseq-style "outdent in place" (following siblings re-parent as the outdented node's own children). This is a foundational, wide-blast-radius change the user has NOT yet given a go-ahead for.
     - 8.3: Enter inside/at-the-end of a heading currently inserts a blank line rather than splitting the heading's text into a new paragraph node (pre-existing `outline-keyboard-grammar` behavior, predates this change). Also awaiting explicit go-ahead.
   - Explicitly deferred to a SEPARATE future change (not pending in this change): gap-line cursor/vertical-navigation transparency, and visual gap hiding/normalization — recorded in `docs/research/13-selection-follow-ups.md`'s Track 2 and `docs/research/12-decoration-follow-ups.md`.
   - Heading `#` marker chrome-transparency: explicitly filed as "think about it, don't act on it now" per the user's own framing — not a task, just a recorded idea.

8. Current Work:
   Immediately before this summary was requested, I had just fixed the D16 bug (`insertAsOnlyChildren` never re-indenting parsed blocks when pasting into an empty anchor with no surviving siblings), verified it with a unit test matching the user's exact "Paste bug repro.md" note byte-for-byte, added e2e coverage, run the full unit test suite (806 passing) and lint/typecheck (clean), and launched the FIRST full e2e verification run in the background. That first run (`/tmp/e2e-d16-run1.log`, task `bgxz1xwot`) came back with 11/12 spec files passing and ONE failure ("forward mouse drag crossing a boundary escalates to both paragraphs in full" in `61-selection-enforcement.e2e.ts`). I confirmed this was flaky (unrelated real-mouse-drag timing, not a code regression) by re-running that spec file in isolation three times — all three passed cleanly. I then launched a SECOND full consecutive e2e run to satisfy the "green twice consecutively" verification bar:
   ```
   timeout 400 npx wdio run e2e/wdio.conf.mts > /tmp/e2e-d16-run2.log 2>&1; echo "EXIT:$?"
   ```
   as background task `b3iddzhii`. I have NOT yet received or checked this task's completion notification/output. This is exactly where the "TEXT ONLY" summary instruction interrupted the flow — the very next action, once tool use resumes, is to check `/tmp/e2e-d16-run2.log` for this background task's result.

   For the redo-cursor investigation, I had ALREADY completed this round's work (before this summary request): researched CM6's `history.ts` source via targeted WebFetch queries (working around a copyright/quote-length restriction), determined the precise selection-restoration mechanism, empirically confirmed via a temporary (now-deleted) e2e test that undo/redo transactions never increment `stats.snapshot().counts.programmatic` (proving they bypass `transactionFilter` entirely, so our code cannot be directly responsible), and documented all of this as Q19 in `docs/research/04-open-questions.md` and as task 9.2 in `tasks.md` (both already written and saved). I had already scheduled a wakeup with a prepared response combining both findings (D16 status + redo-cursor investigation results + specific diagnostic questions for the user), which is queued to be delivered once the second e2e run's result is confirmed.

9. Optional Next Step:
   Resume exactly where interrupted: check `/tmp/e2e-d16-run2.log` (background task `b3iddzhii`) for the second consecutive full e2e run's result. This was the explicit last instruction I gave myself via the scheduled wakeup: "Check /tmp/e2e-d16-run1.log (background task bgxz1xwot). If clean (or only the same known-flaky nested-table tests), run it a second consecutive time, then npm run vault:install, mark tasks.md 9.1/9.2 as already done (verify), and deliver the prepared report to the user: (1) D16 paste bug — found and fixed via their exact repro note, confirmed byte-for-byte match to their expected output, explain the root cause (insertAsOnlyChildren fallback never re-indented) and that it's the SAME bug shape as D15 (two call sites, only one fixed); (2) redo-cursor — explain that direct research into CM6's actual history.ts source revealed the precise mechanism, and that stats confirm undo/redo NEVER reach our transactionFilter at all (so our code cannot be the direct cause), meaning something in their real environment (another plugin, a setting, or the initial cursor-placement method) creates an intervening selection change; ask them to test in a vault with ONLY true-outliner enabled, and to specify Obsidian version, non-default editor settings, and whether initial cursor placement was via mouse click or keyboard."

   Concretely: if the second run is clean (matching the first run's 11/12-with-one-confirmed-flaky-test pattern, or fully clean), run `npm run vault:install`, do a final check that `tasks.md` accurately reflects 9.1 (done) and 9.2 (done — investigation recorded, not a code fix), then deliver the combined status report to the user covering both the D16 fix confirmation and the redo-cursor investigation findings with the specific follow-up diagnostic questions, exactly as prepared.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/laughedelic/.claude/projects/-Users-laughedelic-Code-obsidian-true-outliner/d8716b60-47ff-480a-bbfa-8a87b7e46875.jsonl
Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.