# Code heavy

Intro.

```js
// fences with blank lines inside

function x() {
  return `nested backticks?`;
}
```

- item with code child

  ```python
  def f():
      pass
  ```

- item with

  multiple continuation paragraphs

~~~
tilde fence with ``` inside
~~~

`inline code` paragraph after fence.
